// test.crypto-string.003-no-repeat.ts

/**
 * Shuffrand Test Suite - cryptoString: No Repeat
 *
 * This file specifically tests the `noRepeat` option in `cryptoString`, ensuring that
 * it correctly generates strings without repeating characters, while maintaining the
 * expected character set and length.
 *
 * @author Doron Brayer
 * @license MIT
 */

import { describe, it, expect } from 'vitest'
// Import from the published package path, resolved by tsconfig.test.json paths
import { cryptoString } from 'shuffrand' // Updated import
import { Constants } from 'shuffrand/constants' // Updated import

// Define a top-level group for these tests
describe('cryptoString: No Repeat', () => {
    it('__PLACHOLDER__', () => {
        expect(0.99 + 0.01).toBe(1)
    })
})
